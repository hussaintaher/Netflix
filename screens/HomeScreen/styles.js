import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    categoryText: {
        fontSize: 18,
        color: 'white',
        marginLeft: 10
    },
    categoryWrapper: {
        paddingLeft: 0,
        paddingTop: 10
    }
    
})

export default styles